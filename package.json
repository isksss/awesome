{
  "name": "awesome",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "type": "module",
  "scripts": {
    "check:lessons": "node scripts/check-lessons.mjs",
    "check:links": "node scripts/check-internal-links.mjs",
    "build:data": "node scripts/build-site.mjs",
    "build:site": "pnpm build:data && nuxt generate",
    "build": "pnpm check:lessons && pnpm check:links && pnpm build:site"
  },
  "devDependencies": {
    "@nuxt/ui": "^4.8.2",
    "gray-matter": "4.0.3",
    "markdown-it": "^14.2.0",
    "nuxt": "^4.4.7"
  }
}
